# FIX 12・13 実装計画

## Outcome

複数リージョンと既存コンテナの変更がイベント発生時に plugin から Web DB へ同期され、SSE通知で開いている Web 画面にも自動反映される。

## Scope / decisions

- plugin の同期 HTTP クライアントを HTTP/1.1 に統一し、失敗理由を診断可能にする。
- 起動時に既存リージョンのスナップショットを再送し、失敗中の1リージョンが他リージョンを恒久的に塞がないキューにする。
- ChangePoller の `null` JSON 応答を処理できるようにする。
- Web はコンテナ保存時にリージョンの同期時刻を更新し、SSEで対象セッションへ更新を通知する。定期ポーリングは行わない。
- 通常送信はリージョン作成・対象コンテナ閉鎖（および既存仕様の設置・破壊）を契機に開始し、送信失敗時だけ再試行タイマーを使う。
- 既存API互換性と、ユーザーが追記した `agent/FIX.md` を保持する。

## Acceptance criteria

- 2リージョンのメタデータとコンテナbatchを独立して同期できる。
- plugin再起動後、既存リージョンの全コンテナが再キューされる。
- `nextCursor: null` を含む変更API応答を解析できる。
- 取得済み在庫がサーバー側更新後に再取得される。
- 通常時に10秒ごとのWeb APIポーリングを行わない。
- plugin/Webの関連テスト、型検査、ビルドが通る。

## Work packages

- [x] Plugin: イベント時の即時送信、失敗時再試行、テスト
- [x] Web: SSE通知、UI再取得、ポーリング撤去、テスト
- [x] Integration: 差分レビュー、全検証、実サーバー向け成果物確認
